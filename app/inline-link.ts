import { focusRing } from "./focus-ring";

// An Option name linked inline inside a sentence (a confirm or warning).
export const inlineLinkClass = `font-emphasis text-action underline-offset-2 hover:underline ${focusRing}`;
